import { LogoContainerDirective } from './logo-container.directive';

describe('LogoContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new LogoContainerDirective();
    expect(directive).toBeTruthy();
  });
});
